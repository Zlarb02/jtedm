{% extends "layout/index.njk" %}

{% block content %}
<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-4">Votre panier est pas vide</h1>
    </div>
</div>
<table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Titre du jeu</th>
        <th scope="col">Quantité</th>
        <th scope="col">Prix</th>
        <th scope="col">Total</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th scope="row">1</th>
        <td>{{ game.name }}</td>
        <td>1</td>
        <td>{{ game.price / 100 | round(2)}}</td>
        <td>{{ game.price / 100 | round(2)}}</td>
      </tr>
      <tr>
    </tbody>
  </table>
{% endblock %}